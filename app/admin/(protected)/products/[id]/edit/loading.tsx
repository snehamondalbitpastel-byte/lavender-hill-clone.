import { ProductFormSkeleton } from "@/app/components/admin/AdminSkeletons";

// Edit-product page uses the same big two-column form → same form skeleton.
export default function Loading() {
  return <ProductFormSkeleton />;
}
